import * as test from 'tape';

test('random', t => {
    t.plan(2);

    // const xs = [1, 1, 1, 1];
    // const ixs = uniqueIndexes(xs)(2);

    // t.assert(ixs.length === 2);
    // t.assert(ixs[0] !== ixs[1]);
})